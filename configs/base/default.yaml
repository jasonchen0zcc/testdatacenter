# 默认基础配置
# 被所有任务继承，可通过任务配置覆盖

# 引入公共 Gateway 认证配置
extends:
  - "common/gateway_auth"

# 默认执行配置
execution:
  iterations: 1
  concurrency: 1
  delay_ms: 0
  fail_fast: false
  continue_on_error: true

# 默认超时（秒）
timeout: 300
